export function ToUpperCase(str){
    return str.toUpperCase();
}
export  function ToLowerCase(str){
    return str.toLowerCase();
}
export function captialize_1(str){
    return str.charAt(0)+toUpperCase(str)+slice(1);
}